<?php
    // Operator perbandingan
    // ==, ===, !=, <>. !==, <, <=, >, >=.

    // sama dengan (==)
    var_dump("9" == 9);

    // identik (===)
    var_dump(10 === 10);

    // tidak sama dengan (!=)/(<>)
    var_dump("2" != 100);

    // tidak identik (!==)
    var_dump(10 !== 10)
    
    //
?>